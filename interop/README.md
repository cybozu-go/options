`interop` is a module for testing interop functionality of `options`.
These tests depend on `go-cmp`, `sqlite3`, and so on.
To avoid unnecessary dependencies, these tests cannot be included in the `options` module.
